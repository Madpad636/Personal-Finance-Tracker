package com.financeapp.controller;

// TODO: @WebMvcTest(IncomeController.class) with a mocked IncomeService.
//  - assert 401 without a valid JWT
//  - assert a user cannot fetch/update/delete another user's income (403)
//  - assert validation errors on invalid IncomeRequest return 400
class IncomeControllerTest {
}
